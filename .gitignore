# HyperFrames agent tooling (installed via `npx skills add`) — not part of the repo's source
/.agents/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
!/.claude/hooks/**
/skills-lock.json

# whisper.cpp built from source for local transcription — not part of the repo's source
/.tools/
