# HyperFrames agent tooling (installed via `npx skills add`) — not part of the repo's source
/.agents/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
!/.claude/hooks/**
/skills-lock.json

# Rendered video output — deliverables are handed off, not committed
/renders/
