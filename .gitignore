# HyperFrames agent tooling (installed via `npx skills add`) — not part of the repo's source
/.agents/
/.claude/*
!/.claude/settings.json
!/.claude/hooks/
!/.claude/hooks/**
/skills-lock.json

# Gemini MCP server — secrets and local build/output artifacts
/gemini-mcp/.env
/gemini-mcp/node_modules/
/gemini-mcp/output/
