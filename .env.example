# HeyGen API credentials — copy this file to `.env` and fill in your real key.
#   cp .env.example .env
#
# Get your key from HeyGen: Settings → API  (https://app.heygen.com/settings/api)
# The `.env` file is git-ignored, so your real key is never committed.
HEYGEN_API_KEY=
